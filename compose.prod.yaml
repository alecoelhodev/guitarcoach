services:
  api:
    build:
      target: production
    environment:
      NODE_ENV: production
    ports:
      - "${PORT:-3000}:3000"
    restart: unless-stopped

  postgres:
    restart: unless-stopped
